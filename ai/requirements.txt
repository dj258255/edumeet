# 이 서비스가 실제로 import 하는 것만 둔다. (#134)
#
# 팀 시절 requirements 에는 celery · redis · aiofiles · Pillow · loguru · rich ·
# python-multipart 가 있었는데 ai/*.py 어디에서도 import 되지 않았다.
# 안 쓰는 의존성은 공급망 표면적이다 - 코드는 안 부르는데 CVE 는 따라온다.
#
# starlette 도 뺐다. fastapi 가 자기 버전 범위로 끌어오므로 여기서 못 박으면
# fastapi 를 올릴 때 해결 불가능한 충돌만 만든다.

# --- 웹 프레임워크 ---
fastapi==0.115.12
uvicorn[standard]==0.32.1
pydantic==2.10.4

# --- HTTP 클라이언트 · 설정 ---
#   requests  : backend_client.py 가 Java 내부 API 를 부를 때
#   httpx     : 테스트에서 FastAPI TestClient 가 쓴다
requests==2.32.4
httpx==0.28.1
python-dotenv==1.0.1

# --- LLM ---
#   from openai import OpenAI (신 클라이언트)
openai>=1.50.0

# --- PDF ---
#   fpdf2 는 import 경로가 `from fpdf import FPDF` 다
fpdf2>=2.7,<3

# --- 테스트 ---
pytest>=8.3.4
pytest-asyncio>=0.25.0
responses>=0.25.0
