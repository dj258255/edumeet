# 서버 목록.
#
# ★ 주소를 여기 적지 않는다. (#41)
#   이 저장소는 공개다. "서버 IP + SSH 계정명" 이 같이 있으면 정찰을 돕는다.
#   실제 주소는 group_vars/prod/vault.yml 안에 암호화되어 있고,
#   vars.yml 이 ansible_host 로 연결한다.
#
#   ansible-playbook playbook.yml --check --diff    # 무엇이 바뀌는지만 본다
#   ansible-playbook playbook.yml                   # 적용한다
all:
  children:
    prod:
      hosts:
        oci-aarch64-1: {}
