# 기여 가이드

## 작업 흐름

```
이슈 생성 → 브랜치 생성 → 커밋 → PR → 리뷰 → 머지
```

이슈 없이 `master`에 직접 커밋하지 않습니다.

## 브랜치 전략

```
master
└── <type>/#<이슈번호>-<요약>
```

| 예시 | 용도 |
|---|---|
| `feat/#2-session-type` | 기능 추가 |
| `fix/#7-token-expiry` | 버그 수정 |
| `refactor/#3-remove-port-adapter` | 리팩토링 |
| `perf/#4-n-plus-one` | 성능 개선 |
| `docs/#1-commit-convention` | 문서 |

## 커밋 메시지

```
<type>: <설명>
```

| type | 설명 |
|---|---|
| `feat` | 기능 추가 |
| `fix` | 버그 수정 |
| `refactor` | 리팩토링 (동작 변경 없음) |
| `perf` | 성능 개선 |
| `test` | 테스트 추가/수정 |
| `docs` | 문서 |
| `chore` | 빌드·설정 |

### 규칙

- 타입 뒤에 **콜론과 공백 하나** (`feat: ` / `feat :` 아님)
- 설명은 **무엇을 왜** 바꿨는지. `수정`, `update` 같은 단어만 쓰지 않기
- 커밋 하나는 **한 가지 일**만. 여러 관심사를 섞지 않기

### 좋은 예

```
perf: 과제 목록 조회에서 첨부파일 N+1 제거 - 페치 조인 적용으로 쿼리 101 -> 2
refactor: AssignmentRepository Port 인터페이스 제거 - 구현체가 하나뿐이라 추상화 이득 없음
fix: 세션 정원 검사가 동시 요청에서 초과를 허용하던 문제 - 비관적 락 적용
```

### 나쁜 예

```
수정
update
fix : 버그
```

## PR 규칙

- 제목은 커밋 메시지와 같은 형식
- 본문에 `Closes #<이슈번호>` 를 넣어 이슈와 연결
- **성능 개선 PR은 측정 결과를 반드시 포함** (측정 환경 명시)

## 성능 측정 원칙

응답 시간(ms)이 아니라 **쿼리 수·실행계획**을 1차 지표로 삼습니다.
로컬 측정 시간은 JIT 워밍업·자원 경합·캐시 상태에 좌우되어 신뢰도가 낮지만,
쿼리 수와 실행계획은 환경과 무관하게 재현됩니다.

시간을 함께 기록할 경우 다음을 반드시 병기합니다.

- 측정 환경 (CPU, 메모리, DB 버전)
- 데이터 규모
- 워밍업 횟수와 측정 반복 횟수
- **부하 생성기와 애플리케이션이 자원을 공유하는지 여부**
