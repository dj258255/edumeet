# 데이터소스를 UI 로 등록하면 컨테이너를 다시 만들 때마다 사라진다. 파일로 고정한다. (#28)
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
